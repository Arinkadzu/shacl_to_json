import type { Stream } from '@rdfjs/types';
import type { Environment } from '@rdfjs/environment/Environment.js';
import type { FormatsFactory } from '@rdfjs/formats/Factory.js';
export interface ToFileOpts extends Record<string, unknown> {
    extensions?: Record<string, string>;
}
export default function toFile(env: Environment<FormatsFactory>, stream: Stream, pathOrUrl: string | URL, { extensions, ...options }?: ToFileOpts): Promise<void>;
